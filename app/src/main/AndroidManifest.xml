<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.testclean">

    <application
        android:name=".constant.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.WaveOkActivity"></activity>
        <activity android:name=".ui.TempActivity" />
        <activity android:name=".ui.RandomActivity" />
        <activity android:name=".ui.WaveActivity" />
        <activity android:name=".ui.RandomDrawActivity" />
        <activity android:name=".ui.CleanActivity" />
        <activity android:name=".ui.ScanActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>